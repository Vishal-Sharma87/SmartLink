server{
    listen 80;

    location / {
        proxy_pass http://application:8080;

        # Set the IP of client to the request headers before forwarding 
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Real-IP $remote_addr;
    }
}